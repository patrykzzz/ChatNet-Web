import { AppendTokenInterceptor } from './append-token-interceptor';

describe('AppendTokenInterceptor', () => {
  it('should create an instance', () => {
    expect(new AppendTokenInterceptor()).toBeTruthy();
  });
});
