import { UserRegistrationModel } from './user-registration-model';

describe('UserRegistrationModel', () => {
  it('should create an instance', () => {
    expect(new UserRegistrationModel()).toBeTruthy();
  });
});
